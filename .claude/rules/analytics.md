# Analytics Service

**Stack**: Python 3.12, FastAPI, XGBoost, asyncpg
**Role**: Blockchain data fetching, graph building, risk scoring

---

## Implemented ✅
- `POST /api/analyze` — main analysis endpoint (analytics/app/api/analyze.py)
- FastAPI/Pydantic `RequestValidationError` (`422`) wrapped into structured `ErrorResponse` via global handler in `main.py`; shared `ErrorResponse`/`_error()` live in `analytics/app/api/errors.py`
- `GET /api/networks` — list supported networks
- `GET /api/model/status` — model load status (k8s readiness probe)
- `GET /health` — healthcheck
- `UniversalXGBoostScorer` — hybrid signal (flag heuristic + XGBoost), `MODEL_VERSION = "universal_xgboost_v1"`, one model for all 10 networks
- Scaler: log1p + z-score, loads from `models/btc_scaler.json`, fallback to hardcoded stats if missing
- Fallback: heuristic-only scoring if model file missing (no crash)
- Address format validation: `app/validators/address.py` — per-network regex
- Fetchers: BTC (mempool.space), ETH (Etherscan v2), TRX, SOL (Helius), BNB (Moralis), XRP, LTC, DOGE, ADA (Blockfrost), TON (TonCenter)
- DB connection pool: asyncpg (main.py lifespan)
- Request lifecycle: `processing → completed / failed`
- Persistence foundation for history: analysis requests/results are saved; public history API belongs to web-app
- Config: `analytics/app/config.py` — database_url, max_depth=5, max_addresses=20, API keys
- `period_days` enforcement: `analyze.py` computes `since_ts` from `now - timedelta(days=period_days)` and passes it to `GraphBuilder.build(since_ts=...)`. The builder discards transactions with `tx.timestamp < since_ts` after each fetch (builder-side filtering; no provider-side filtering is applied).
- Structured human-readable risk factors are generated by `analytics/app/scoring/factors.py` from computed features, DB flags, and final score metadata. This is rule-based explainability over visible signals, not SHAP or model-internal XGBoost explanation.
- `AnalyzeResponse.EdgeOut` exposes aggregated edge time range as nullable Unix timestamp seconds: `first_seen` and `last_seen`.
- Explicit feature-vector validation before scoring: `UniversalXGBoostScorer` verifies `AddressFeatures` field order against `OUR_FEATURE_NAMES`, vector length, finite values, semantic ranges, scaler stats for all log-normalized features, and loaded booster feature names when exposed by XGBoost.
- Feature-vector validation failure is an internal scoring failure. It propagates through the generic API error path as structured `INTERNAL_ERROR`; raw validation details are not exposed to API clients, and validation failure does not trigger heuristic fallback.
- **Test baseline established**: `analytics/requirements-dev.txt` pins `pytest`, `pytest-asyncio`, and `httpx` for dev/test use (separate from runtime `requirements.txt`). `analytics/tests/conftest.py` provides session/function-scoped fixtures: `minimal_graph_result`, `empty_graph_result`, `no_flags`, `one_flag_ransomware`, `zero_features`, `loaded_scorer`. All 30 existing tests pass (30/30). No DB, Docker, or Postgres dependency introduced.
- **Feature extraction unit tests**: `analytics/tests/test_feature_extraction.py` covers canonical feature order (`OUR_FEATURE_NAMES` vs `AddressFeatures` field order, count == 27), `to_numpy()`/`to_dict()` order preservation, empty-graph zero behavior and `min_dist_to_flagged = 999` sentinel, volume group (tx_count aggregation, total received/sent, median/max as per-edge averages — current behavior locked), topology group (in/out degree, depth1/depth2 neighbor counts from `GraphResult.nodes`, graph density formula), temporal group (lifespan_days min-1 invariant, active_days from timestamp day-buckets, tx_per_day, zero-timestamp guard), and risk-signal group (flagged count/ratio/distance, per-category increments, case-insensitive flag matching, out-of-graph addresses ignored, multiple categories on one address). No DB, Docker, or external dependency introduced.
- **Factor generation unit tests**: `analytics/tests/test_factors.py` (82 tests) covers schema shape (all five required keys on every factor, all severities valid), database path (root_flag present emits `root_address_flagged`, value reflects flag_type, severity inherits `score_result.risk_level` for LOW/MEDIUM/HIGH, `features=None` returns only the root-flag factor), clean/empty output (`features=None` + no root_flag returns `[]`; `zero_features` + no flags returns `[]`), flagged-neighbor behavior (count/addresses in value, sorted addresses, cap at 10, MEDIUM/HIGH severity tiers), low-distance-to-flagged (distances 1/2/3 emit with HIGH/MEDIUM/LOW severity; 4/5/999 suppressed), elevated flagged-neighbor ratio (below 0.10 suppressed; 0.10–0.24 MEDIUM; ≥0.25 HIGH), all 8 category-specific flags parametrized (emit/suppress, correct severity and value), high-transaction-frequency (threshold >20, LOW/MEDIUM/HIGH tiers), many-unique-counterparties (threshold ≥20, tiered severity), high received/sent volume (threshold ≥10, tiered severity). No DB, Docker, network, async, FastAPI, or model-artifact dependency.
- **Scorer unit tests**: `analytics/tests/test_scorer.py` (39 tests) covers: (A) `_validate_feature_vector` direct calls — valid zero vector passes, NaN/+inf/-inf raises, negative non-ratio feature raises, ratio/density/coefficient above 1 and below 0 raises, missing scaler stat raises, sigma == 0 / sigma < 0 / sigma == inf raises, mu == NaN raises, booster feature-name mismatch raises, `feature_names=None` skips check, model without `feature_names` attr passes, field order mismatch raises, vector length mismatch raises; (B) heuristic fallback — scorer at nonexistent path has `is_model_loaded == False`, `model_version == "universal_xgboost_v1_heuristic"`, zero features → score 0.0 / risk_level LOW, flag and proximity features increase score deterministically, score capped at 100, same features produce same result; (C) skip-guarded smoke test when real model artifacts are present — `is_model_loaded == True`, score returns valid `ScoreResult` in [0, 100] with correct risk_level. Groups A and B have no DB, Docker, network, async, FastAPI, or model-artifact dependency; monkeypatching via pytest `monkeypatch` (function-scoped) prevents global state leakage between tests.
- **API success-path tests**: `analytics/tests/test_api_success.py` (37 tests) uses FastAPI `TestClient` with `asyncpg.create_pool` patched so the lifespan does not connect to a real DB. All five mock targets (`GraphBuilder.build`, `repo.get_flagged_addresses`, `repo.get_address_flag`, `repo.save_analysis`, `repo.mark_request_completed`) are patched per test class; the initial `pool.execute(INSERT analysis_requests)` is handled by an `AsyncMock` pool injected at startup. No Postgres, no blockchain API, no model artifacts required. Covers: `GET /health` (200 + body), `GET /api/networks` (200 + 10 network codes), `GET /api/model/status` (200 + valid `loaded`/`heuristic_fallback` values for all 10 networks), `POST /api/analyze` database path (root address flagged → `scoring_method=database`, `model_version=database_lookup`, `flag_type` set, `features={}`, nodes/edges/factors present), `POST /api/analyze` ML path (root address not flagged → `scoring_method=ml_model`, `model_version` contains `xgboost`, `features` dict populated with 27 keys, edges include `first_seen`/`last_seen`, factors field present). The real heuristic scorer runs on the ML path — no model artifact needed. DB persistence, container/k8s coverage, and real model artifact verification are not claimed here.
- **API error-contract tests**: `analytics/tests/test_api_errors.py` (6 tests) uses FastAPI `TestClient` with `asyncpg.create_pool` patched so startup does not connect to a real DB, plus an `AsyncMock` pool for the initial `analysis_requests` insert. Covers all structured non-200 `/api/analyze` contracts: `INVALID_REQUEST` → 422, `INVALID_ADDRESS` → 400, `UNSUPPORTED_NETWORK` → 400, `BLOCKCHAIN_RATE_LIMITED` → 429, `BLOCKCHAIN_UNAVAILABLE` → 502, and `INTERNAL_ERROR` → 500. Tests verify exact error response keys, `request_id` null before DB record creation, `request_id` present after DB record creation, failed post-insert requests call `repo.mark_request_failed`, and raw internal exception detail is not exposed. No Postgres, blockchain API, model artifact, Docker, or k8s dependency is introduced.
- **Postgres repository integration smoke tests**: `analytics/tests/test_repository_postgres_integration.py` (3 tests) are skipped unless `TEST_DATABASE_URL` is set and never use normal `DATABASE_URL`; verified against a temporary real PostgreSQL 16.4 database via `TEST_DATABASE_URL`. The tests initialize schema by extracting the `01_schema.sql` literal block from `k8s/postgres/initdb-configmap.yaml` with stdlib parsing. Coverage is repository/Postgres only: successful ML-path persistence through `analysis_requests` → `analysis_results` → `address_nodes` → `graph_edges` → `analysis_features` plus `mark_request_completed`; failed request persistence via `mark_request_failed`; and network-aware `get_address_flag` / `get_flagged_addresses` lookup using the same raw address on different networks. No Docker, container build, k3s, FastAPI endpoint, blockchain API, or model artifact verification coverage is claimed.
- **Model artifact verification tests**: `analytics/tests/test_model_artifacts.py` (7 tests) verify real artifacts when present and skip those checks when absent. Coverage: `analytics/models/btc_xgboost.json` and `analytics/models/btc_scaler.json` presence when available, scaler key coverage for `_LOG_FEATURES`, finite `(mu, sigma)` values with `sigma > 0`, loaded booster feature-name alignment with `OUR_FEATURE_NAMES`, real `UniversalXGBoostScorer` load/score smoke when artifacts are present, missing-model heuristic fallback without real artifacts, and corrupted-scaler scorer-level `ScoringValidationError` using temp files only. Analytics model artifacts may be committed intentionally for reproducibility/backup; secrets, private API keys, database credentials, and `.env` files must not be committed. No Docker, container, k8s, deployment, FastAPI endpoint, blockchain API, Postgres, or external-service coverage is claimed.
- **Local analytics container smoke support**: `analytics/Dockerfile` builds the FastAPI service on `python:3.12-slim`, installs only runtime dependencies from `analytics/requirements.txt` (not `requirements-dev.txt`), sets `WORKDIR /app` so `from app...` imports work and relative `models/btc_xgboost.json` / `models/btc_scaler.json` resolve, copies analytics source including model artifacts when present, exposes port 8000, and starts `uvicorn main:app --host 0.0.0.0 --port 8000`. Root `.dockerignore` excludes git metadata, `.env*`, virtualenvs, caches, and build/test artifacts while allowing `analytics/models/*.json`. `scripts/smoke-analytics-container.sh` builds the image from repo root, starts a temporary PostgreSQL 16.4 container on a private Docker network, starts analytics with `DATABASE_URL=postgresql://riskapp:riskapp_secret@<postgres-container>:5432/riskscoredb`, requires no blockchain API keys or real `.env` for startup, waits for `/health`, and calls `/api/model/status`. The smoke verifies container build, runtime dependency install, FastAPI startup/lifespan DB connectivity to temporary Postgres, and both local endpoints only; no k3s, container registry, Kubernetes deployment, schema migration, or external blockchain API coverage is claimed.
- **Analytics Kubernetes manifest readiness files**: `k8s/analytics/` contains pre-deploy manifests for analytics-service: Deployment, internal ClusterIP Service, ConfigMap, Secret, and README. Deployment `analytics-service` uses image `risk-score-analytics:latest` with `imagePullPolicy: IfNotPresent` for local k3s image-import workflows, exposes container port 8000, reads `DATABASE_URL` from `analytics-secret`, reads `BTC_MODEL_PATH`, `MAX_DEPTH`, and `MAX_ADDRESSES_PER_ANALYSIS` from `analytics-config`, and reads optional blockchain API keys from Secret refs marked optional. Probes are configured as startup `/health`, liveness `/health`, and readiness `/api/model/status`; Kubernetes checks HTTP status only and does not inspect the model-status JSON body. Conservative resources are set (`requests: 250m CPU / 512Mi`, `limits: 1000m CPU / 1Gi`). Model artifacts are expected baked into the image by `analytics/Dockerfile`; no model volume, ConfigMap, Secret, or initContainer is mounted. The Service `analytics-service` is ClusterIP-only on port 8000. No Ingress, k3s apply/deploy, registry push, live cluster validation, or staging smoke coverage is claimed.
- **k3s staging deploy smoke verified (2026-04-26)**: Built `risk-score-analytics:latest` from `analytics/Dockerfile`, imported it into local k3s/containerd with `docker save risk-score-analytics:latest | sudo k3s ctr images import -`, confirmed the image in `sudo k3s ctr images list`, applied `k8s/namespace.yaml` idempotently, verified existing PostgreSQL in namespace `risk-score-app` (`postgres` Deployment and Pod ready, Service/Secret/PVC/initdb ConfigMap present, `select count(*) from networks;` returned 10), applied analytics ConfigMap/Secret/Deployment/Service manifests, and confirmed `analytics-service` rolled out with pod `1/1 Running` and zero restarts. The live staging DB initially lacked canonical column `analysis_requests.period_days`; this schema drift was fixed in staging with `ALTER TABLE analysis_requests ADD COLUMN IF NOT EXISTS period_days INTEGER;`, and the column was verified as nullable integer before rerunning the smoke. Logs showed clean Uvicorn startup, Kubernetes probes hit `/health` and `/api/model/status`, and no raw secrets were observed in logs. A temporary port-forward to `svc/analytics-service` verified `/health` returned `{"status":"ok"}` and `/api/model/status` reported all 10 networks as `loaded` (no `heuristic_fallback`). A real low-scope BTC `/api/analyze` request for `1BoatSLRHtKNngkdXEeobR76b53LETtpyT` with `period_days=30` completed with HTTP 200 using `scoring_method="ml_model"`, `model_version="universal_xgboost_v1"`, `risk_level="MEDIUM"`, 2 nodes, 1 edge, and persisted DB rows: `analysis_requests.status='completed'`, `analysis_requests.period_days=30`, `analysis_results` row, 2 `address_nodes`, 1 `graph_edges`, and 1 `analysis_features`. A safe pre-DB unsupported-network smoke returned HTTP 400 structured `UNSUPPORTED_NETWORK` with `request_id: null` and did not create a new `analysis_requests` row. Application runtime behavior remains aligned with the canonical schema: `period_days` is accepted, used for filtering, and persisted.

## NOT Implemented ❌
- [ ] Clean up pre-existing Pydantic v2 deprecation warning in analytics tests
- [ ] Add explicit `NO_TRANSACTIONS_FOUND` / `DATA_INSUFFICIENT` handling for empty transaction graphs instead of scoring all-zero feature vector as normal ML result
- [ ] Normalize EVM addresses consistently before graph construction and feature extraction so checksum/lowercase variants of the same address are not treated as separate nodes
---

## Analysis Algorithm
```
1.  Receive request (address, network, depth, tx_limit, period_days)
2.  Save analysis_request to DB with status=processing
4.  Fetch tx data from blockchain API
5.  Build transaction graph (BFS up to depth)
6.  Match all graph nodes against flagged_addresses DB
7.  Is root address in flagged DB?
    YES → get flag_type + risk_level from DB → go to step 9 (skip ML)
    NO  → extract 27 numerical features from graph
        → build feature vector (OUR_FEATURE_NAMES order)
        → validate feature length/order, finite values, semantic ranges, scaler stats, and booster feature names
        → apply log1p + z-score normalization
        → run XGBoost model (output: probability 0–1 → scale to 0–100)
        → classify risk_level by thresholds:
            LOW    = score < 25
            MEDIUM = 25 ≤ score < 60
            HIGH   = score ≥ 60
9.  Build final result (risk_score, risk_level, factors, nodes, edges)
10. Save analysis_result + address_nodes + graph_edges to DB
11. Set status=completed, return result to client
```

---

## ML Feature Vector (27 features, fixed order)
Defined in `analytics/app/graph/features.py` → `OUR_FEATURE_NAMES`

**Volume (7)**
| Feature | Description |
|---|---|
| tx_in_count | Incoming tx count |
| tx_out_count | Outgoing tx count |
| total_received | Total incoming volume (native currency) |
| total_sent | Total outgoing volume |
| median_tx_amount | Median tx amount |
| max_tx_amount | Max single tx amount |
| unique_counterparties | Unique counterparty addresses |

**Topology (6)**
| Feature | Description |
|---|---|
| depth1_neighbors | Node count at depth 1 |
| depth2_neighbors | Node count at depth 2 |
| in_degree | In-degree of root node |
| out_degree | Out-degree of root node |
| graph_density | edges / max possible edges |
| clustering_coefficient | Clustering coefficient (undirected projection) |

**Temporal (3)**
| Feature | Description |
|---|---|
| active_days | Unique calendar days with transactions |
| tx_per_day | Average tx frequency |
| lifespan_days | Days between first and last tx |

**Risk signals (11)**
| Feature | Description |
|---|---|
| flagged_neighbors_count | Flagged address count in graph |
| flagged_neighbors_ratio | Fraction of flagged among all nodes |
| min_dist_to_flagged | Shortest path to nearest flagged node (999 = none) |
| flag_mixer | Mixer-flagged neighbour count |
| flag_scam | Scam-flagged neighbour count |
| flag_sanctions | Sanctions-flagged neighbour count |
| flag_darknet_market | Darknet market-flagged neighbour count |
| flag_ransomware | Ransomware-flagged neighbour count |
| flag_gambling | Gambling-flagged neighbour count |
| flag_phishing | Phishing-flagged neighbour count |
| flag_suspicious | Suspicious-flagged neighbour count |

Model: `analytics/models/btc_xgboost.json`
Scaler: `analytics/models/btc_scaler.json`

---

## API Contract

```python
# AnalyzeRequest
address: str            # min 10, max 128 chars, format validated per network
network: str            # BTC/ETH/TRX/SOL/BNB/XRP/LTC/DOGE/ADA/TON (uppercased)
depth: int              # 1–5, default 2
tx_limit: int           # 1–200, default 50
period_days: int | None # 1–3650, optional

# AnalyzeResponse (HTTP 200 — success only)
request_id, result_id, address, network
risk_score: float       # 0.00–100.00
risk_level: str         # LOW / MEDIUM / HIGH
model_version: str      # "universal_xgboost_v1" | "universal_xgboost_v1_heuristic" | "database_lookup"
scoring_method: str     # "ml_model" | "database"
flag_type: str | None   # set when scoring_method == "database"
nodes_count, edges_count
nodes: list[NodeOut]
edges: list[EdgeOut]    # one entry per directed address pair (aggregated)
# EdgeOut: from_address, to_address, tx_count, total_amount, first_seen, last_seen
# first_seen / last_seen: int | None, nullable Unix timestamp seconds
factors: list[dict]     # structured human-readable factors; may be empty
features: dict          # set when scoring_method == "ml_model"
analyzed_at: str        # ISO datetime
```

### Factor Object

`AnalyzeResponse.factors` and `analysis_results.factors_json["factors"]` contain a list of objects with this schema:

```python
key: str          # stable machine-readable factor key
label: str        # short human-readable title
value: Any        # raw value or evidence value that triggered the factor
severity: str     # LOW / MEDIUM / HIGH
description: str  # human-readable explanation
```

Factors fire for directly flagged root addresses, flagged neighbors, low distance to flagged addresses, elevated flagged-neighbor ratio, specific risk categories (`sanctions`, `mixer`, `scam`, `darknet_market`, `ransomware`, `gambling`, `phishing`, `suspicious`), high transaction frequency, many unique counterparties, and high received/sent volume. Clean low-signal analyses may return `factors: []`.

### Structured Error Response (all non-200 cases)

All error responses return JSON with this shape regardless of HTTP status:

```json
{
  "error_code": "<machine-readable string>",
  "detail": "<human-readable message>",
  "request_id": "<UUID string or null>"
}
```

| `error_code` | HTTP status | Cause |
|---|---|---|
| `INVALID_REQUEST` | 422 | FastAPI/Pydantic schema validation failed (wrong types, missing fields, out-of-range values) |
| `INVALID_ADDRESS` | 400 | Address fails per-network format validation |
| `UNSUPPORTED_NETWORK` | 400 | Network code not in supported set |
| `BLOCKCHAIN_RATE_LIMITED` | 429 | All upstream providers returned HTTP 429 |
| `BLOCKCHAIN_UNAVAILABLE` | 502 | All upstream providers failed (timeout, 5xx, network error) |
| `INTERNAL_ERROR` | 500 | Unexpected internal failure (DB error, scoring bug, etc.) |

- `request_id` is `null` for `INVALID_REQUEST` — the `RequestValidationError` is raised by FastAPI before the endpoint body executes, so no `analysis_request` DB record is ever created.
- `request_id` is `null` for errors detected before the DB record is created (INVALID_ADDRESS, UNSUPPORTED_NETWORK).
- `request_id` is present for errors detected after the DB record is created (BLOCKCHAIN_*, INTERNAL_ERROR) — the failed request is always persisted.
- Raw internal exception text is NEVER included in `detail`. The internal reason is logged server-side and persisted in `analysis_requests.error_message`.

---

## Constraints
- ❌ NEVER exceed max_depth=5 or max_addresses_per_analysis=20
- ❌ NEVER change AnalyzeRequest/AnalyzeResponse schema without updating this file
- ❌ NEVER retrain model inline — training is in `analytics/training/` only
- ✅ risk_level: exactly `LOW`, `MEDIUM`, `HIGH` — no other values
- ✅ Always save analysis_request to DB BEFORE calling blockchain API
- ✅ Early exit if root address in flagged_addresses — skip ML entirely
- ✅ Blockchain fetchers MUST raise `BlockchainRateLimitedError` on HTTP 429 and `BlockchainUnavailableError` when all providers fail — returning `[]` on total failure is FORBIDDEN (it is indistinguishable from a legitimate empty address)
- ✅ Partial success within a fetch (e.g. one TX in a batch fails) may still return `[]` for that TX only; a full provider outage must raise
- ✅ Fallback to heuristic if model file missing — do not crash
- ✅ Feature-vector validation failures are internal scoring failures — do not sanitize values and do not fall back to heuristic-only mode
- ✅ All async DB operations via app.state.db_pool (asyncpg)
- ❌ NEVER hardcode DB credentials — use config.py / environment variables
- ❌ NEVER commit `.env`, private API keys, database credentials, or secrets. Analytics model artifacts may be committed intentionally for reproducibility/backup.
- ❌ NEVER expose raw exception text or SQL errors in API error responses — use structured ErrorResponse only
