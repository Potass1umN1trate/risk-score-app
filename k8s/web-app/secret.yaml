apiVersion: v1
kind: Secret
metadata:
  name: web-app-secret
  namespace: risk-score-app
type: Opaque
stringData:
  # Placeholder values for local/pre-deploy readiness only.
  # Replace all secret material before deploying to shared environments.
  DATABASE_URL: "postgresql://riskapp:riskapp_secret@postgres:5432/riskscoredb"
  AUTH_SECRET: "dev-auth-secret-2026-05-01-local-k3s-web-app-8f3b9e6c2a1d4f70"
  GITHUB_CLIENT_ID: "dev-github-oauth-disabled"
  GITHUB_CLIENT_SECRET: "dev-github-oauth-disabled"
  ADMIN_EMAIL: "admin@example.com"
  ADMIN_PASSWORD: "LocalDevAdminPassword-2026!"
