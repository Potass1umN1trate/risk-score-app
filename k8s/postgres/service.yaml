apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: risk-score-app
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP — доступен только внутри кластера
  # web-app и analytics подключаются как postgres:5432
  type: ClusterIP
